/**
 * LoginHeader - Suppressed on auth pages to avoid duplicate headers,
 * overlapping controls, and vertical overflow.
 */
const LoginHeader = () => {
  return null;
};

export default LoginHeader;
